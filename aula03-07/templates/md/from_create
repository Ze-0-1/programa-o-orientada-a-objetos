<!DOCTYPE html>
<html lang="pt-br">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Formulário de Modelos</title>
   <style>
       /* Estilos aplicados diretamente aos campos de entrada */
       input[type="text"], select {
           width: 400px;
           padding: 8px;
           margin-bottom: 15px;
           border: 2px solid #c0c0c0;
           border-radius: 10px;
           font-size: 1em;
       }


       /* Estilos aplicados ao botão */
       button {
           padding: 10px 20px;
           background-color: #007bff;
           color: white;
           border: none;
           border-radius: 4px;
           cursor: pointer;
       }


       button:hover {
           background-color: #0056b3;
       }

   </style>
</head>
<body>


   <h1>Novo Modelo</h1>


   <form action="/md/create" method="post">
       <label for="nome">Nome do Modelo:</label><br>
       <input type="text" id="nome" name="nme_modelo" required>
       <br><br>
       <label for="montadora">Montadora do Modelo:</label><br>
       <select id="montadora" name="cod_montadora">
           {% for m in lst %}
              <option value="{{ m.idt_montadora }}">{{ m.sgl_montadora }} - {{ m.nme_montadora }}</option>
           {% endfor %}
       </select>
       <br><br>


       <button type="submit">Salvar</button>
   </form>


   <div style="display: {{ display }};">{{ msg }}</div>
   <hr>
   [ <a href="/">Voltar ao Menu</a> ]
</body>
</html>
